<!--
Before opening: read CONTRIBUTING.md. New endpoints, retries, caching, batching,
and an async client are out of scope here — open an issue first.
-->

**What this changes and why** (not just what — the diff shows what)

**Checklist**

- [ ] One logical change; unrelated fixes split into their own PR
- [ ] `pytest` and `ruff check` pass locally
- [ ] README updated, if user-facing behavior changed
- [ ] `CHANGELOG.md` entry added under `Unreleased`
- [ ] Commits are signed off (`git commit -s`) per the DCO
